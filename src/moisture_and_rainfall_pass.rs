use crate::wind_column_gradient_map;

pub fn gen_rainfall_map(terrain_map: &Vec<Vec<f64>>, wind_column_gradient_map: Vec<Vec<Option<(usize, usize)>>>) -> Vec<Vec<f64>> {

    return vec![vec![0.0; 1]; 1];
}